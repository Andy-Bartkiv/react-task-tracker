import MyButton from "./MyButton";

const Header = () => {
    return (
        <header className="header">
            <h2>Task Tracker</h2>
            <MyButton text = 'Add'/>
        </header>
    )
}

export default Header;
